// Cloudflare deployment — a Workers project serving only static assets, the
// same pattern as the two studios.
//
//   npm run build          # → dist/
//   npx wrangler deploy    # → https://lakolam-www.<account>.workers.dev
{
  "name": "lakolam-www",
  "compatibility_date": "2026-08-01",
  "assets": {
    "directory": "./dist"
  }
}
